# 2026-08-22 — 44 — Phase 4 opens: harden, document the method, refresh the highlights, write the test guide

- **Date**: 2026-08-22
- **Tool**: Claude Code
- **Model**: Claude Fable 5
- **Phase**: 07-hardening (Phase 4 open)
- **Intent**: Start Phase 4 against plan 04 as written; use and document our own multi-agent loop for the one adversarial pass D2 reserved it for; make sure the video highlights are current; produce the guide for Pablo's own unaided test, which doubles as script material.
- **In English**: "Let's start Phase 4, hardening — do and document everything we use (especially if we use our own loop; read plan 04 if it is aligned). Since we are close to producing the videos, check that the script highlights are up to date and complete them with this task's findings. And create a guide for my real testing, which will also serve the script. Thanks!"

## Prompt

```text
bien vamos a comenzar con la Fase 4 de hardening haciendo y documentando todo lo que usamos (especialmente si usamos nuestro propio loop (leer el plan 04 si esta alienado). y como ya estamos cerca de la produccion de videos, verificar que los highlights para guiones esten actualizados, completarlos con los hallazgos de esta tarea. y crear una guia para mi testeo real, que tambien va a servir para el guion. Gracias!
```

## How the loop was used (the orchestration prompts, paraphrased — they were issued by the session, not by Pablo)

Three background agents in parallel, read-only, over the whole repo, each told: *B1–B27 are
already known, report only new defects; file:line, concrete input → wrong behaviour,
severity, confidence, and whether you verified it by reading the exact path (quote it);
max 10, ranked; no style nits.* Lenses: correctness · security · stack idiomatic-ness.
A fourth agent, on disjoint files, added the English summary to the 64 prompt entries that
lacked one. The session then re-read every finding against the code, deduplicated across
lenses, ran the hostile sweep against a live server (port 3100 / Postgres 5433), and
triaged: fix four, register fourteen.

## Outcome

Plan 04 was aligned and was followed as written. Fixed and committed (`466dc29`): the
quadratic HTML stripper (3.8 s → 21 ms on the measured case, output byte-identical), the
mid-body fetch failure that left a run `processing`, 413/415 collapsed into "malformed",
and the timeout copy that claimed a retry the code never made. Registered B28–B41. Secret
scan clean. Prompt log: 64 English summaries (`7f0b029`). D27 records the pass, the sweep
table and the production-failure-modes list. Highlights 54–59 appended.
`plan/guides/manual-test-guide.md` written for 4.5 and for the walkthrough. Pablo's timed
fresh-clone run is the one item still owed, and it is his by design.
