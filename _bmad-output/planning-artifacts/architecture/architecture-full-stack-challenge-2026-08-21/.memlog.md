---
scope: Full app architecture: Menu Extraction & Review (menulens) — one Fastify+TS service, React+Vite front, Postgres+Drizzle, OpenAI SDK
purpose: Build substrate for epics/stories; closes PRD open items (single test, SSRF, native PDF, HEIC)
altitude: feature→epics
updated: 2026-08-21T18:07
---

- (direction) Session start (worktree bmad/architecture-session). Inputs: PRD final 2026-08-21 (FR1-FR36, T1-T6) + addendum ADR/mechanics + DECISIONS D1-D15. Pablo's standing instruction this session: RISKS.md and REQUIREMENTS.md are binding scope guardrails — every AD must map to a lighthouse line (§1-§2/§6) or be cut. Upstream challenge repo re-checked at session start: HEAD 6be4b93, zero issues.
- (constraint) [ADOPTED] Stack fixed by challenge: Node+Fastify+TS, Postgres+Drizzle (real migration), React+Vite+TS+Tailwind+shadcn/ui, OpenAI SDK (JSON mode + vision), Zod at API and LLM boundaries, Pino structured logs, exactly ONE automated test. Not re-decidable.
- (constraint) [ADOPTED] Over-engineering guardrails (REQUIREMENTS §4, auto-reject risk R-06): no microservices/k8s/event bus, no queues (BullMQ/Redis), no SSE/WebSocket, no auth, no monorepo tooling, no deploy infra; Docker Compose for local Postgres only; stock shadcn components.
- (constraint) [ADOPTED] Processing model inherited from PRD/D13: persist-first + client polling (TanStack Query), in-process continuation, ONE technical timeout (OpenAI call ~120s), staleness derived at read time (>3min), retry = new run. Cut list stands: queue/worker, SSE, dynamic ETA, resumable extraction, idempotency keys, reaper.
- (question) OPEN-1 (R8): single-test choice + formal justification for DECISIONS.md. Front-runner: deterministic arbiter T1-T6 incl. T6 downgrade path (deterministic, zero API cost, crosses contract→triage→persistence, encodes the asymmetric gate).
- (question) OPEN-2 (FR36): SSRF guard mechanics for URL fetch — private/internal/local address refusal; must stay dependency-light; also 'what breaks in production' material.
- (question) OPEN-3 (E6): verify OpenAI native PDF input on the web — a zero-dep yes eliminates E6 (scanned PDF unsupported); until verified E6 stands.
- (question) OPEN-4 (FR1): verify iOS HEIC→JPEG auto-conversion on upload; if verification fails the answer stays E4's error — no conversion library (cut at PRD reviewer gate).
- (decision) Session mode: Coaching path (elicitation visible, feeds BMAD-fluency evidence). Deliverable: spine only — ARCHITECTURE-SPINE.md + memlog + DECISIONS.md closures; no extra renderings (guardrail §4). Chosen by Pablo at activation.
- (decision) Topology (Pablo): one Fastify service + one Vite SPA + one Postgres. Binds: no other runtime pieces exist; prevents: accidental second service/worker/proxy sneaking in (guardrail §4).
- (decision) Shared contract package (Pablo): FE and BE import one shared package holding the Zod schemas / types (Dish, Run, allergen enum, API payloads). Duplicated types rejected as dirt/maintainability hazard. Binds: single source of contract truth; prevents: front and back drifting on the Dish shape.
- (direction) Session plan (Pablo): sweep ALL architecture questions now, then a final validation pass via bmad-advanced-elicitation before Finalize, to surface anything Pablo isn't seeing.
- (decision) Backend paradigm (Pablo): functional core / imperative shell. Arbiter T1-T6, T6 evidence verification, price parsing = pure functions, no IO (module in server, signals in → flag+reasons out). Fetch/OpenAI/Postgres live in the shell. Prevents: triage logic entangled with infrastructure; makes the single test natural.
- (decision) Single test (R8, Pablo): Vitest unit test over the pure arbiter T1-T6 incl. T6 downgrade path. Justification for DECISIONS.md: deterministic, zero API cost, crosses extraction-contract→triage, encodes the product's central promise (asymmetric allergen gate). Playwright E2E and integration-with-fake-LLM considered and declined (plumbing over judgment; fragility; DB-in-CI).
- (decision) Source artifacts (Pablo): uploaded PDF/image bytes + acquired source text persisted in Postgres (bytea/text columns), served via endpoint with Content-Type. One store, survives fresh clone, no path management. Filesystem+path rejected (second stateful place, README risk).
- (decision) Data shape (Pablo): dishes.allergens ({id, provenance, evidence_quote, verified}) and dishes.confidence_reasons persisted as jsonb on the dish row, shape governed by shared Zod schemas. Normalized child tables rejected: v1 has no per-allergen queries (FR32) — joins without a rubric row behind them.
- (direction) Derivable positions shown and not vetoed so far: npm workspaces server/web/shared (plain npm, no pnpm — README<5min); polling contract POST /api/runs → id, GET /api/runs/:id; derived states (interrupted, done) computed server-side at read, never stored; front server-state only in TanStack Query, no global store; SSRF = dep-free dns.lookup + private-range blocklist + per-redirect re-validation + http(s)-only + fetch size/time caps; Pino via Fastify native logger, stage transitions + fired T rules logged.
- (version) Stack versions verified against npm registry 2026-08-21: fastify 5.12.1, drizzle-orm 0.45.2, drizzle-kit 0.31.10, vite 8.2.2, tailwindcss 4.3.3, @tanstack/react-query 5.101.4, zod 4.4.3, openai 7.5.0, vitest 4.1.11, react 19.2.8, pino 10.3.1, @fastify/multipart 10.1.1, typescript 7.0.2. Spine seeds 'current at scaffold time'; these are the reference snapshot.
- (decision) OPEN-4 CLOSED (HEIC, FR1): web-verified — Safari auto-converts HEIC→JPEG on upload when the input's accept list excludes HEIC; Safari 17+ pitfall: including image/heic in accept can convert files TO heic. Rule: accept=image/jpeg,image/png,image/webp (never image/heic) + application/pdf on the PDF input. Raw .heic (macOS drag) → E4 error, no conversion lib (PRD gate cut stands). Zero deps.
- (event) OPEN-3 fact verified (native PDF): OpenAI Responses API accepts PDFs as input_file (base64 / Files API id / URL) on vision-capable models; API extracts text+page images server-side; caps 100 pages / 32MB per request. Consequence surfaced: native-only path leaves no local ground text → T6 machine-verification and FR23 'what the system read' tab would be lost on PDFs. Decision pending Pablo: text-layer / native-only / hybrid class-based.
- (decision) OPEN-1 CLOSED — single test REVISED (Pablo, supersedes earlier unit-arbiter entry): integration golden-master. POST a demo fixture to the real API, OpenAI client mocked, real Postgres; poll to completion; assert normalized final payload against golden. Rationale: one-test budget rewards breadth — crosses API contract, persist-first, Zod LLM boundary, JSONB round-trip, arbiter, derived states; E2E via real OpenAI rejected (uncontrollable non-determinism); pure unit rejected (never sees the Drizzle/JSONB boundary). Conditions: (1) fixture crafted to fire every T1-T6 rule incl. T6 downgrade + one reliable row, golden asserts flag+reasons per row; (2) OpenAI client behind an injectable seam — becomes an AD; (3) golden normalization rule: ids/timestamps frozen or excluded, stable ordering; (4) CI checks job adds a Postgres service container (still D12-minimal). Visible course-correction from PRD front-runner, D10-style.
- (decision) OPEN-3 CLOSED — PDF path (Pablo): hybrid by class. pdfjs-dist (pure JS) extracts text layer during fetching_source (ms-scale, negligible vs 30-90s model call — latency question raised and answered with numbers). NEW INVARIANT: every run is classified text|visual by presence of trustworthy ground text (URL and PDF-with-text-layer = text; image and scanned PDF = visual). Model input, T6 machine-verification scope, and FR23 'what the system read' tab ALL key on class, never on file type. Scanned PDF → native PDF input_file to OpenAI, Ana verifies visually → E6 ELIMINATED with honest downgrade. Native-only rejected (gate weakened on all PDFs); text-layer-only rejected (leaves scanned unsupported for no reason). PRD surgical update owed at close: E6 removal + T4/T6 wording by class.
- (direction) All four PRD open items now closed (test golden-master, SSRF dep-free blocklist, PDF hybrid-by-class, HEIC accept-list). Next per Pablo's plan: bmad-advanced-elicitation validation pass over the full decision set, then Finalize (distill → reconcile → reviewer gate).
- (decision) Elicitation rule 1 (class threshold): class=text iff extracted text layer ≥ a minimum-chars threshold (exact value calibrated on dev menus; the rule's existence is the invariant); pdfjs parse error ⇒ class=visual (native PDF to API; API rejection ⇒ failed with reason). Closes the 'text layer present but garbage' seam.
- (decision) Elicitation rule 2 (T6 normalization pinned): quote-vs-source matching normalizes BOTH sides identically: Unicode NFKC → lowercase → collapse whitespace → strip diacritics. Exact list lives in the spine; two builders normalizing differently = different flags on same data (worst possible divergence).
- (decision) Elicitation rule 3 (source type by content): after URL fetch, source handling is decided by the FINAL response content-type (a URL redirecting to a PDF takes the PDF path), never by which input field was used.
- (decision) Elicitation rule 4 (schema family): shared package holds ONE base Zod schema per entity; model-output, persisted, and API-payload variants are derived via .pick()/.extend()/.omit(), never re-declared. Prevents triple-declared Dish inside shared itself.
- (decision) Elicitation rule 5 (artifact isolation): source artifacts (bytes + acquired text) live in their own table; artifact bytes are NEVER selected in run/dish list queries; served exclusively by a dedicated endpoint with correct Content-Type + X-Content-Type-Options: nosniff, accepted MIME types only.
- (decision) Elicitation rule 6 (server-side seriality): FR35 enforced by the server — POST /api/runs while a run is active returns 409; the UI mirrors this truth, never owns it.
- (decision) Elicitation rule 7 (test + prompt discipline): golden-master = ONE scenario, ONE fixture, ONE golden (reads as one test, never a smuggled suite); the runtime extraction prompt is a versioned file surfaced in prompts/ (R11 covers every prompt to any LLM, runtime included).
- (decision) Elicitation rule 8 (SSRF residual named): implemented mitigation = dep-free blocklist + per-redirect re-validation + http(s)-only + size/time caps; DNS rebinding (TOCTOU) documented as accepted residual risk — 'what breaks in production' material, not silently ignored.
- (decision) Elicitation rule 9 (sequencing + error containment): (a) the surgical PRD update (E6 elimination, T4/T6 wording by class) happens at THIS session's close — prerequisite for the epics session, not a courtesy; (b) every stage transition ends in a persisted state or silence-caught-by-staleness — the failure handler itself never throws (Postgres-down double-failure case).
- (event) Advanced elicitation pass complete: 5 methods (Assumption Audit, Critical Perspective, Second-Order, Cascading Failure, Boundary Sweep) + integration. Verdict: decision set RATIFIED (zero reversals), RECTIFIED by addition — 9 seam-closing rules approved by Pablo. Also ratified under attack: class invariant, pdfjs-vs-HEIC-lib coherence, hostile-model containment by deterministic gate.
- (event) Reviewer Gate complete: 6/6 pass-with-fixes, zero critical, zero over-engineering findings, zero contradictions. Convergent fixes: dish description+provenance missing from ERD (3 reviewers); T6 offsets must be persisted and reused by the web (never a second matcher); NORMALIZATION BUG caught by adversarial lens — strip-diacritics after NFKC is a no-op, correct order NFKC→lowercase→NFD→remove combining marks→collapse whitespace; pre-run rejections E1/E4/E5 = 4xx without run row; 'active' for the 409 = processing AND not yet stale (deadlock fix); runs.status vs runs.stage split defined; dishes persisted atomically at saving, GET returns [] mid-run, golden freezes done payload; ONE review mutation path incl. reopen; failure-reason enum listed explicitly, E6 gone; acquired text served text/plain (XSS); dish position column; react-router decided (deep-linking FR30); honest-UI + uploads convention rows; E6 leftovers purged; AD-6/AD-12 cite in-session web verifications.
- (version) Gate verification updates: pdfjs-dist v6 requires its optional @napi-rs/canvas (prebuilt; npm-only holds; NEVER install with --omit=optional; Node floor ≥22.13). OpenAI PDF caps current docs: 50 MB per file/request, no documented page limit (memlog's earlier 100p/32MB was the old figure; our 10 MB app cap binds first). openai 7.5.0 + zod 4.4.3 zodTextFormat verified working by execution (peer ^3.25||^4) — scaffold-compat deferred item CLOSED. shadcn CLI 4.18.0 + Tailwind 4.3.3 + Vite 8 + React 19 confirmed compatible.
- (event) spine finalized — status: final, 14 ADs; gate 6/6 pass-with-fixes applied; PRD amended in place (E6 retired, T4/T6/FR19 by class, open items closed); D16-D18 + R-13 recorded; session prompts logged (prompts/03-bmad-architecture, 10 entries); video highlights 19-24 appended. Next: bmad-create-epics-and-stories.
